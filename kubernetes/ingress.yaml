# Ingress for nginx ingress controller.
# For a different controller (e.g. traefik), adjust annotations and spec accordingly.
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: familyeye-server
  annotations:
    nginx.ingress.kubernetes.io/proxy-body-size: "50m"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "60"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "60"
spec:
  ingressClassName: nginx
  rules:
    - host: familyeye.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: familyeye-server
                port:
                  number: 8443
  # tls:
  #   - hosts:
  #       - familyeye.example.com
  #     secretName: familyeye-tls
